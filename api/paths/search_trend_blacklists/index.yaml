summary: List Search Trend Blacklists
operationId: listSearchTrendBlacklists
tags:
  - Search Trend Blacklists
security:
  - basicAuth: []
description: You must be Admin+.
parameters:
  - $ref: "../../components/parameters/limit.yaml"
  - $ref: "../../components/parameters/page.yaml"
  - $ref: "../../components/parameters/order.yaml"
  - in: query
    name: tag
    schema:
      type: string
  - in: query
    name: reason
    schema:
      type: string
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/searchTrendBlacklist.yaml"
  403:
    $ref: "../../components/responses/accessDenied.yaml"
