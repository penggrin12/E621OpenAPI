summary: List Search Trends
operationId: listSearchTrends
tags:
  - Search Trends
parameters:
  - in: query
    name: day
    schema:
      type: string
      format: date
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/searchTrend.yaml"
